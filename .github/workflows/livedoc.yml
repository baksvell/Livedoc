name: Living Documentation Check

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  livedoc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install livedoc
        run: pip install -e .

      # Проверка: при изменении кода без обновления доки или --update job fails
      - name: Check documentation freshness
        run: python -m livedoc.cli examples --docs docs
